import styled from "@emotion/styled";
import Ball from "./Ball";

const CommunityButton = styled(Ball)`
  width: 200px;
  height: 200px;
  transform: translate(-180%, -155%);
`;

export default CommunityButton;
