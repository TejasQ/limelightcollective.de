import React, { FC } from "react";
import styled from "@emotion/styled";

import Ball from "./Ball";

const SpaceButton = styled(Ball)`
  width: 140px;
  height: 140px;
  transform: translate(160%, -265%);
`;

export default SpaceButton;
